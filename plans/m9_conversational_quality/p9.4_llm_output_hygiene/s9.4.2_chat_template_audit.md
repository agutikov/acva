# S9.4.2 — Chat template + special-token audit

**Phase:** P9.4 (LLM output hygiene)  
**Estimate:** 3-4 days

## Context

Each GGUF in the registry ships with its own chat template
(Qwen2.5, Mistral, Outlier-Lite, …). llama-server picks one up
from the GGUF metadata, but:

- Some templates inject hidden system messages we don't see.
- Special tokens (BOS / EOS / `<|im_start|>` / `<|im_end|>`)
  occasionally leak into output if the template doesn't terminate
  cleanly.
- EOS isn't always honored — model continues past the natural
  stop, producing run-on replies.

This stage audits the templates of every shipped LLM alias and
locks down the prompt path so the operator knows exactly what's
being sent.

## Tasks

### Task T9.4.2.1 — Inspect each shipped GGUF

For each alias in `config/default.yaml` `models.llm.*`:

```sh
./_build/dev/acva memory restart   # via model-controller, if smoke-tested
# Or, manually with llama-server's --verbose-prompt flag:
docker compose exec llama llama-server --model /models/<file>.gguf --verbose-prompt …
```

Capture the actual prompt llama-server constructs. Compare to
the prompt our `PromptBuilder` thinks it's sending. Note any
hidden-system-message injection, special-token misalignment, or
EOS misconfiguration.

Record findings in a new `docs/reports/llm_chat_templates.md`.

### Task T9.4.2.2 — Explicit chat-template override

For models where the embedded template does something we don't
want (e.g., injects "You are a helpful assistant" when our
personality already supplied a system prompt), use llama-server's
`--chat-template` arg to force a known template. Wire as
`cfg.llm.chat_template_override` (string or empty for "use
embedded").

### Task T9.4.2.3 — EOS / stop-token configuration

Validate stop-token IDs match across:

- The GGUF's metadata (`tokenizer.ggml.eos_token_id`).
- llama-server's `/v1/chat/completions` accepting standard
  stop sequences.
- `PromptBuilder` not accidentally putting `<|im_end|>` in
  user content (sanitize input).

Add a `tests/test_prompt_special_token_leak.cpp` that asserts
no special token is emitted in `LlmSentence.text`.

### Task T9.4.2.4 — `--verbose-prompt` plumbing

llama.cpp's `--verbose-prompt` server flag prints every
constructed prompt. Add `cfg.supervisor.llm_verbose_prompt:
false` (default off), wire to llama-server startup args via the
model-controller pre-call (S9.0.3 work). Operators can flip on
during dialog quality debugging without rebuilding compose.

### Task T9.4.2.5 — Documentation

`docs/guide/operations.md` § "Inspecting the LLM prompt" — how
to enable verbose-prompt, where the templates live, how to
override.
