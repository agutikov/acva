# S11.5.2 — Advanced operator + architecture + internals docs

**Phase:** P11.5 (tool authoring + docs)
**Estimate:** 1 week
**Status:** stage placeholder.
**Shape:** docs deliverable.

## Context

`ideas.md` line 87: "docs, readme for advanced users plus arch
overview, internals in docs".

M8C step 3 already shipped `docs/architecture.md`,
`docs/guide/configuration.md`, `docs/guide/operations.md`.
Those are the "intermediate operator" surface. The
"advanced operator" (the runtime engineer per the audience
matrix in S11.5.3) needs deeper internals — threading model,
event flow per turn, FSM transition table, AEC math, memory
schema, model-controller protocol.

## Tasks

### T11.5.2.1 — Internals docs index

`docs/internals/README.md` — index page mapping subsystem
to deep-dive page.

### T11.5.2.2 — Threading-model deep-dive

`docs/internals/threading.md` — every thread, what it owns,
which events cross which boundaries, lock discipline. Cites
file:line for the canonical implementation.

### T11.5.2.3 — Event-flow per turn

`docs/internals/event_flow.md` — the path of one turn from
mic to speaker, with each event published + consumed.
Living diagram, regenerated when events change.

### T11.5.2.4 — Memory schema

`docs/internals/memory_schema.md` — every SQLite table,
foreign keys, lifecycle states, retention policy.

### T11.5.2.5 — FSM table

`docs/internals/fsm.md` — full transition table with
guards, actions, and the events that fire each.

### T11.5.2.6 — Model-controller protocol

`docs/internals/model_controller.md` — REST surface,
state machine, edge cases.

## Resolves / depends

- Pairs with M11.P11.5 tool-authoring docs; same audience
  ("runtime engineer" per the S11.5.3 taxonomy).
- No new question.
