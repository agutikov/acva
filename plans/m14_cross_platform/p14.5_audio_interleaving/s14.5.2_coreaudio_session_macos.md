# S14.5.2 — CoreAudio session ducking on macOS

**Phase:** P14.5 (audio interleaving)
**Estimate:** 1 week
**Status:** stage placeholder.
**Shape:** investigation + impl, gated on M14.P14.1.

## Context

`ideas.md` line 95 + macOS port. macOS's audio session
model is different from Linux; `AVAudioSession` (iOS-style)
has explicit `duckOthers` modes. On macOS proper, the
mechanism is per-process audio policy.

## Tasks

### T14.5.2.1 — API survey

Document the macOS-correct path:
- `AudioObjectSetPropertyData` with
  `kAudioHardwarePropertyMixStereoToMono` and friends?
- `AVAudioSession` if running through the iOS-on-macOS
  compatibility layer?
- Voice-Processing IO unit (already used for AEC) — does
  it expose ducking?

### T14.5.2.2 — Implementation

Whichever the survey says is canonical. Wrap behind the
same `cfg.audio.ducking` config block as Linux.

### T14.5.2.3 — Acceptance

Same as S14.5.1 but on macOS — Spotify ducks when acva
speaks.

## Resolves / depends

- Gated on M14.P14.1 macOS port.
- Feeds Q27.
