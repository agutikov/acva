# S13.0.1 — M8C packaging stretch (absorbed)

**Phase:** P13.0 (M8C packaging stretch)
**Estimate:** 1-2 weeks
**Status:** stage placeholder.

## Origin

Items originally parked at MVP close as
`plans/postpone/m8c_packaging_stretch.md`. That file was
folded into this stage and removed 2026-05-08; git history
preserves it. The original "when to revive" criteria still
apply — pick up this phase when there's a second operator
or a distro-policy review.

## Tasks

### T13.0.1.1 — Image digest pinning

`packaging/compose/docker-compose.yml` `:server-cuda` /
`:latest-cuda` → `@sha256:...`. Document the bump procedure
in `scripts/dev-up.sh`.

### T13.0.1.2 — Compose project-label cleanup

Rename observability project to `acva-obs`. Update
`dev-up.sh` / `dev-down.sh` to match; remove the polling-by-
container-name workaround.

### T13.0.1.3 — `packaging/man/acva.1`

Auto-generate via `help2man` from `acva --help`. Wire into
the build (release builds emit the man page; install
recipe installs to `${prefix}/share/man/man1/`).

### T13.0.1.4 — Fresh-VM bare-metal acceptance

Stand up a clean Manjaro VM and a clean Ubuntu 24.04 VM.
`git clone && ./build.sh && ./scripts/install-systemd.sh &&
systemctl --user start acva.target`, then `acva demo
health`. Document any VM-specific gotchas in
`packaging/systemd/README.md`.
