# S13.3.2 — Design-history single-file export tooling

**Phase:** P13.3 (CI/CD + release engineering)
**Estimate:** 3-4 days
**Status:** stage placeholder.
**Shape:** tooling task.

## Context

`ideas.md` lines 5-7 + `plans/open_questions.md` Q14:

> convert plans and docs history into single-file design
> with history of acva creation; process it and load into
> memory + add memory read instructions into context, so
> agent can answer questions about itself.

Today's `docs/` and `plans/` are organised for human
navigation. A future agent loading acva's history wants a
single corpus with stable ordering; this stage builds the
exporter.

## Tasks

### T13.3.2.1 — Crawl + concatenate

`tools/export-design-history/` (Python). Walks `plans/` +
`docs/history/` + `docs/architecture.md` + `CLAUDE.md` in
a deterministic order, concatenates into one
`design_history.md` with section anchors.

### T13.3.2.2 — Embedding + chunking

Optional: chunk the corpus + embed via Speaches' embedding
endpoint, store as a side-table for the auto-memory
system.

### T13.3.2.3 — gh-pages publish

Render the corpus as a single-page HTML, deployed to
gh-pages alongside the docs site (T13.3.1.4).

### T13.3.2.4 — Memory-load instructions

`docs/agent-onboarding.md` — operator-facing instructions
for loading the corpus into Claude Code / Anthropic SDK
memory at session start.

## Resolves / depends

- Resolves Q14 (tooling).
- Pairs with T13.3.1.4 gh-pages publish.
- No new question.
