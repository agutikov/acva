# S13.1.1 — AUR / .deb / .rpm

**Phase:** P13.1 (Linux native packages)
**Estimate:** 2-3 weeks
**Status:** stage placeholder.

## Tasks

### T13.1.1.1 — AUR PKGBUILD

`packaging/aur/acva/PKGBUILD` for `acva-git` (rolling) and
`acva` (release-tag-tracking). Builds against the system
deps named in `README.md`. Submit to AUR.

### T13.1.1.2 — `.deb` build

`packaging/deb/build.sh` driving `debuild`. Some deps differ
from Arch (`libwebrtc-audio-processing-dev`, etc.). Source-
build instructions for opentelemetry-cpp + glaze if not in
24.04 apt repos.

### T13.1.1.3 — `.rpm` build

For Fedora / RHEL / openSUSE. `packaging/rpm/acva.spec` +
build script. May discover system-package gaps; document.

### T13.1.1.4 — Acceptance

Each package installs cleanly on a fresh VM. `man acva` +
`systemctl --user enable acva.target` work post-install.
